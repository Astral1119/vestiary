# contract

[`SPEC.md`](./SPEC.md) is the theme contract — **frozen v1.0, normative**.
Read it first; everything else in the repo implements or consumes it.

Design history (how the contract got its shape) lives in
[`../docs/THEME-LOOP-DESIGN.md`](../docs/THEME-LOOP-DESIGN.md); where the
two disagree, SPEC.md wins.
